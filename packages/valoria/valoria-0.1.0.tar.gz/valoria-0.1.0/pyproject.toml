[project]
name = "valoria"
version = "0.1.0"
description = "Outils pour gérer fichiers: structure, tmp, permissions, nettoyage, dépendances, chmod"
authors = [{name = "GameurDev"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = []

[project.scripts]
fichier = "valoria.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
