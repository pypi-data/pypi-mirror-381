[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "XVideoDownloader"
version = "0.1.0"
description = "Python wrapper for X (Twitter) video downloader API"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  {name = "0600011", email = "09192006a@gmail.com"}
]
dependencies = [
  "requests>=2.0.0"
]

[project.urls]
Homepage = "https://github.com/yourname/XVideoDownloader"