[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
dynamic = ["version"]
name = "setch-py"
requires-python = ">=3.10"
authors = [{ name = "mentiferous" }]
description = "Utility for retrieving system information on Linux"
readme = "README.md"
license = "GPL-3.0-or-later"
keywords = ["setch", "setch-py", "linux", "utility"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Natural Language :: English",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Utilities",
]

[project.urls]
Source = "https://github.com/mentiferous/setch.git"

[project.scripts]
setch = "setch.__main__:main"

[tool.hatch.version]
path = "src/setch/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/setch"]

[tool.isort]
profile = "black"
