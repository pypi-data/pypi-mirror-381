"""
Setup script for ZyraCrypt

This is a minimal setup.py for compatibility with legacy tools.
All configuration is in pyproject.toml (PEP 517/518).

For modern installations, use:
    pip install .
    pip install -e .  # editable/development mode
"""

from setuptools import setup

# All configuration is in pyproject.toml
setup()
