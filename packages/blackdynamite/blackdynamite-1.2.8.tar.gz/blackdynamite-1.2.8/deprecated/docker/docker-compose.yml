version: '3.3'

services:

   blackdynamite:
     build: "."
     volumes:
       - blackdynamitedata:/var/lib/postgresql/
     restart: always
     ports:
       - "5432:5432"
volumes:
    blackdynamitedata:
