[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fp-datatables"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "build>=1.3.0",
    "sqlalchemy>=2.0.43",
    "twine>=6.2.0",
]

[project.urls]
Homepage = "https://github.com/Rohit-kumar-raja/fastapi-datatables"
