from enum import auto

from nf_common_base.b_source.common.configurations.datastructure.b_enums import (
    BEnums,
)


class BieSnapshotsColumnNames(BEnums):
    NOT_SET = auto()

    BIE_STAGE_OF_OBJECT_CONTENT_BIE_IDS = (
        auto()
    )

    BIE_RELATIVE_PATH_PARENT_IDS = (
        auto()
    )

    BIE_STAGE_OF_FILE_REFERENCE_NUMBER_IDS = (
        auto()
    )

    BIE_STAGE_OF_RELATIVE_PATH_IDS = (
        auto()
    )
