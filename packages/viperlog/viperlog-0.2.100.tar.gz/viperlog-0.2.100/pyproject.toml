
[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "viperlog"
version = "0.2.100"
description = "ViperLog core logging package"
readme = "README.md"
requires-python = ">=3.12"
#license = { text = "MIT" }
license = "MIT"

[tool.setuptools.dynamic]
version = {attr = "viperlog.__version__"}


[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]


[project.optional-dependencies]
core = ["viperlog"]
# Refered to in the errormessages are:
# console, http
# the 0.2.100 constant is replaced by the CI/CD pipeline
console = ["viperlog-console-plugin==0.2.100"]
http = ["viperlog-http-plugin==0.2.100"]
dnxlogging = ["viperlog-dnxlogger-plugin==0.2.100"]
all = ["viperlog-console-plugin==0.2.100", "viperlog-http-plugin==0.2.100","viperlog-dnxlogger-plugin==0.2.100"]

