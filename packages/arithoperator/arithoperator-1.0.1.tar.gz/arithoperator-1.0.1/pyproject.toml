[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "arithoperator"
version = "1.0.1"
description = "A simple Python package to perform arithmetic operations on multiple numbers."
readme = "README.md"
requires-python = ">=3.7"
authors = [
    { name = "Bineesh Shaji", email = "bineeshshaji@gmail.com" }
]
license = { text = "MIT" }
keywords = ["arithmetic", "math", "numbers", "calculator"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]
dependencies = []

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools.packages.find]
where = ["."]

[project.scripts]
arithop = "arithop.__main__:main"

[project.urls]
"Homepage" = "https://github.com/bineeshs/arithop"
"Source" = "https://github.com/bineeshs/arithop"
"Tracker" = "https://github.com/bineeshs/arithop/issues"

