[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kalibr"
version = "0.1.16"
description = "Kalibr SDK Demo"
authors = [{name="Devon Kelley"}]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
]