#!/usr/bin/env scriptthing-run
# lang: py
from scriptthing.utils import store
import sys
if __name__ == '__main__':
    if len(sys.argv) < 3:
        sys.exit(1)
    key = sys.argv[1]
    value = sys.argv[2]
    store.put(key, value)
    print('OK')