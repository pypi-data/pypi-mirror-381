[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "erabytse-ghost"
version = "0.1.0"
description = "An ethical companion for retiring digital accounts — not deletion, but closure."
authors = [{name = "takouzlo", email = "f.bfalik@gmail.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Utilities",
]

[project.scripts]
erabytse-ghost = "erabytse.ghost:main"

