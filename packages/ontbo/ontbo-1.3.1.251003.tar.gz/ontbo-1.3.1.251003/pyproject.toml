[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "ontbo"
version = "1.3.1.251003"
description = "Python client for the Ontbo API. Go-to https://www.ontbo.com/."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Stéphane Aubry", email = "contact@ontbo.com" }]
dependencies = [
    "requests",
]

[tool.setuptools.packages.find]
where = ["ontbo"]

[project.urls]
Homepage = "https://github.com/Ontbo/ontbo-context-api"
Issues = "https://github.com/Ontbo/ontbo-context-api/issues"