[project]
name = "exotic-bikes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sid Anand", email = "Sid.Anand@walmart.com" }
]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.5",
]

[project.scripts]
exotic-bikes = "exotic_bikes:main"

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"
