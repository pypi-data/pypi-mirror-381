[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "erabytse-tomb"
version = "0.1.0"
description = "A respectful cemetery for dead links — not deletion, but remembrance."
authors = [{name = "takouzlo", email = "f.bfalik@gmail.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.25.0",
]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Internet :: WWW/HTTP",
    "Intended Audience :: End Users/Desktop",
]

[project.scripts]
erabytse-tomb = "erabytse.tomb:main"