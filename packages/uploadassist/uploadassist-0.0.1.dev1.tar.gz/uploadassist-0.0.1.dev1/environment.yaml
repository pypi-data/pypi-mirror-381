name: uploadassist-dev
channels:
  - conda-forge
  - defaults
dependencies:
  - python >=3.9
  - pip
  - setuptools
  - wheel
  - setuptools-scm
  - pytest
  - pytest-cov
  - coverage
  - pip:
      # Add any other dependencies here as needed
