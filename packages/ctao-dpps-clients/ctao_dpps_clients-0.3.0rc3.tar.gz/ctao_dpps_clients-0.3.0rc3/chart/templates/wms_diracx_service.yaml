apiVersion: v1
kind: Service
metadata:
  name: wms-diracx
spec:
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/instance: dpps
    app.kubernetes.io/name: diracx
  sessionAffinity: None
  type: ClusterIP
