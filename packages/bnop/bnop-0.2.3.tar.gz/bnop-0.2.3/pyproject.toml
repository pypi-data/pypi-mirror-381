[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "bnop"
version = "0.2.3"
description = "a common top level ontology for bCLEARer based on BORO."
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "Mesbah Khan", email = "khanm@ontoledgy.io" }]
dependencies = []
