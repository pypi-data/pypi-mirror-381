[tool.poetry]
name = "com-terra-logger"
version = "0.1.0"
description = "Reusable logger utility"
authors = ["Telephone.com"]
readme = "README.md"
license = "MIT"

[tool.poetry.dependencies]
python = "^3.12"
colorama = "^0.4.6"

[[tool.poetry.packages]]
include = "logger"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
