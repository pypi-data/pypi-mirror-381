[tool.poetry]
name = "str_sim_scorer"
version = "3.1.0"
description = ""
authors = ["Devin McCabe <dmccabe@broadinstitute.org>"]
readme = "README.md"
packages = [{include = "str_sim_scorer"}]

[tool.poetry.dependencies]
python = ">=3.9"
pandas = ">=1.4"
numpy = ">=1.26.3"
scipy = ">=1.12.0"

[tool.poetry.group.dev.dependencies]
ruff = "^0.12.7"
pyright = "^1.1.404"
pyarrow = "^21.0.0"

[tool.poetry.group.test.dependencies]
pytest = "^7.4.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.ruff]
select = [
    "I",  # isort formatting
]

[tool.pyright]
venvPath = "."
venv = ".venv"
